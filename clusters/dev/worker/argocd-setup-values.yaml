argo-cd:
  global:
    domain: argocd.dev-worker.nubes.stfc.ac.uk

longhorn:
  ingress:
    host: "longhorn.dev-worker.nubes.stfc.ac.uk"
  persistence:
    # can't be set for RWX
    migratable: false