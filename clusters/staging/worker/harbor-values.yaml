stfc-cloud-harbor:
  harbor:
    externalURL: "https://harbor.staging-worker.nubes.stfc.ac.uk"

    expose:
      ingress:
        hosts:
          core: "harbor.staging-worker.nubes.stfc.ac.uk"

    database:
      type: external
      external:
        host: "dbspgha03.fds.rl.ac.uk"
        coreDatabase: "cloud_harbor_registry_dev"

  backup:
    enabled: true
    sourceBucket: "s3://harbor-bucket"
    destination:
      daily: "s3://harbor-backup/daily"
      weekly: "s3://harbor-backup/weekly"
      monthly: "s3://harbor-backup/monthly"
    endpoint: "https://s3.echo.stfc.ac.uk"