{{- if .Values.internal.basicAuth.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: internal-basic-auth
  namespace: monitoring-system
type: Opaque
stringData:
  # htpasswd format - generate with: htpasswd -nb <user> <password>
  credentials: {{ .Values.internal.basicAuth.credentials }}
{{- end }}